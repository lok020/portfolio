import { render, screen } from '@testing-library/react';
import Body from '../Component/Body';

const page = "home";

test('render Body.js', () => {
    // const { no_item } = render(<Body current_page={page} changePage={()=>{}} scroll_page={false} endScroll={()=>{}}/>);
    // expect(no_item).toBeValid;
});

