import { render, screen } from '@testing-library/react';
import About from '../Component/About';

test('render About.js', () => {
    // render(<About/>);
    // const text = screen.getByText("Personal Information");
    // expect(text).toBeInTheDocument();
});

